import {createMuiTheme} from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#fff'
	},
	secondary: {
	  main: '#57b846',
	  contrastText: '#fff'
	},

  },
});
export default theme
